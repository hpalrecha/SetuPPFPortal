// Placeholder customer contact helpers. The implementation lives in the shared
// module so client and server stay in lockstep; this re-export keeps existing
// server import paths (e.g. storage.ts) working.
export * from '../../shared/placeholderContact';
